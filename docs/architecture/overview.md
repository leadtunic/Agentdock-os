# Architecture Overview

AgentDock OS is a modular self-hosted platform.

```txt
Dashboard / Toolbar / CLI / Channels -> API -> Runtimes -> Tools -> Audit
```

Services include API, agent-runtime, browser-runtime, gateway, worker, memory-engine, skill-engine and sandbox-runner.
